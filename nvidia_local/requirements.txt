# NVIDIA CUDA PyTorch (CUDA 12.1 compatible)
# Fixed for Python 3.14 compatibility

# PyTorch with CUDA support (using latest available for cu121)
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1
--index-url https://download.pytorch.org/whl/cu121

# FastAPI server (uvicorn without httptools extra to avoid Windows wheel issues)
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0

# Image/data processing
requests>=2.31
Pillow>=10.0
numpy>=1.26
opencv-python>=4.8.0

# 3D model processing
trimesh>=4.0
scipy>=1.11

# Configuration/utils
python-dotenv>=1.0

# These are needed by the model repos but should be installed separately:
# transformers (for Depth Anything V2)
# segment-anything (for SAM)
# mmengine + mmsegmentation (for DFormer, optional)
# open3d (for point cloud processing, optional)
